name: frappe
description: A functional reactive programming library for Dart with scoped state management.
version: 1.0.0
homepage: https://github.com/fromlabs/frappe

environment:
  sdk: '>=3.5.0 <4.0.0'

resolution: workspace

dev_dependencies:
  test: ^1.25.0
  lints: ^6.1.0
