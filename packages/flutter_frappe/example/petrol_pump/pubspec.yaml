name: petrol_pump
description: Petrol Pump FRP example — pure Dart business logic.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.5.0

dependencies:
  frappe:

resolution: workspace

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
